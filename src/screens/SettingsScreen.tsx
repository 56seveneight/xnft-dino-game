import { Text } from "react-native";

import { Screen } from "../components/Screen/Screen";

export function SettingsScreen() {
  // @TODO - Add current wallet address
  // @TODO - Add button to save current score to blockchain (to the metadata for the current NFT) such that each NFT would eventually have it's own score attached to it.
  return (
    <Screen>
      <Text>TODO - SETTINGS</Text>
    </Screen>
  );
}
